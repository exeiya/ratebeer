<h2>List of ratings</h2>

<% if @ratings.empty? %>
  <p>No ratings yet.</p>
<% else %>
  <p>Number of ratings: <%= @ratings.count %></p>
<% end %>

<ul>
  <% @ratings.each do |rating| %>
    <li> <%= rating %>
    <%= link_to rating.user.username, user_path(rating.user.id) %></li>
  <% end %>
</ul>

<% if current_user %>
  <%= link_to "New Rating", new_rating_path, class:'btn btn-primary' %>
<% end %>